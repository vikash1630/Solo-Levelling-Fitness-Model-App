<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log - Player Sign In</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/output.css">

    <style>
        @keyframes pulse-glow {
            0%, 100% {
                box-shadow: 0 0 20px rgba(59, 130, 246, 0.6),
                            0 0 40px rgba(59, 130, 246, 0.4),
                            inset 0 0 20px rgba(59, 130, 246, 0.2);
            }
            50% {
                box-shadow: 0 0 40px rgba(96, 165, 250, 1),
                            0 0 80px rgba(59, 130, 246, 0.7),
                            inset 0 0 30px rgba(59, 130, 246, 0.3);
            }
        }

        @keyframes slide-down {
            0% {
                transform: translateY(-100px);
                opacity: 0;
            }
            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes energy-flow {
            0% {
                transform: translateX(-100%);
            }
            100% {
                transform: translateX(100%);
            }
        }

        @keyframes border-pulse {
            0%, 100% {
                border-color: rgba(59, 130, 246, 0.5);
                box-shadow: 0 0 15px rgba(59, 130, 246, 0.4);
            }
            50% {
                border-color: rgba(96, 165, 250, 1);
                box-shadow: 0 0 30px rgba(96, 165, 250, 0.8);
            }
        }

        @keyframes rotate-slow {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }

        @keyframes text-shimmer {
            0%, 100% {
                text-shadow: 0 0 20px rgba(147, 197, 253, 1),
                            0 0 40px rgba(59, 130, 246, 0.8);
            }
            50% {
                text-shadow: 0 0 30px rgba(191, 219, 254, 1),
                            0 0 60px rgba(96, 165, 250, 1);
            }
        }

        @keyframes particle-rise {
            0% {
                transform: translateY(0) scale(1);
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
            100% {
                transform: translateY(-150px) scale(0);
                opacity: 0;
            }
        }

        @keyframes input-glow {
            0%, 100% {
                box-shadow: 0 0 10px rgba(59, 130, 246, 0.3);
            }
            50% {
                box-shadow: 0 0 20px rgba(96, 165, 250, 0.6);
            }
        }

        @keyframes corner-expand {
            0%, 100% {
                width: 24px;
                height: 24px;
            }
            50% {
                width: 32px;
                height: 32px;
            }
        }

        .slide-down {
            animation: slide-down 0.8s ease-out forwards;
        }

        .pulse-glow {
            animation: pulse-glow 2.5s ease-in-out infinite;
        }

        .border-pulse {
            animation: border-pulse 3s ease-in-out infinite;
        }

        .energy-bar {
            position: relative;
            overflow: hidden;
        }

        .energy-bar::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, 
                transparent, 
                rgba(191, 219, 254, 1), 
                rgba(147, 197, 253, 1),
                transparent);
            animation: energy-flow 2s linear infinite;
        }

        .text-shimmer {
            animation: text-shimmer 2.5s ease-in-out infinite;
        }

        .particle {
            animation: particle-rise 5s ease-in-out infinite;
        }

        .rotating-ring {
            animation: rotate-slow 20s linear infinite;
        }

        .corner-accent {
            animation: corner-expand 2s ease-in-out infinite;
        }

        input:focus {
            animation: input-glow 2s ease-in-out infinite;
            outline: none;
        }

        input::placeholder {
            color: rgba(147, 197, 253, 0.4);
        }

        .custom-gradient-bg {
            background: linear-gradient(135deg, #000000 0%, #0a1628 50%, #020817 100%);
        }

        .hex-pattern {
            background-image: 
                radial-gradient(circle at 20px 20px, rgba(59, 130, 246, 0.1) 1px, transparent 1px);
            background-size: 40px 40px;
        }

        .glass-morph {
            background: rgba(15, 23, 42, 0.4);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
        }

        /* Mobile optimizations */
        @media only screen and (max-width: 640px) {
            .slide-down {
                animation: none !important;
            }

            .text-shimmer {
                animation: none !important;
                text-shadow: 0 0 15px rgba(147, 197, 253, 0.8) !important;
            }

            .border-pulse {
                animation: none !important;
                border-color: rgba(59, 130, 246, 0.6) !important;
                box-shadow: 0 0 12px rgba(59, 130, 246, 0.4) !important;
            }

            .pulse-glow {
                animation: none !important;
                box-shadow: 0 0 12px rgba(59, 130, 246, 0.4) !important;
            }

            .energy-bar::before {
                animation: none !important;
                opacity: 0.4 !important;
            }

            .rotating-ring {
                animation: none !important;
            }

            .corner-accent {
                animation: none !important;
            }

            .particle {
                display: none !important;
            }

            .hex-pattern {
                opacity: 0.3 !important;
            }

            .glass-morph {
                backdrop-filter: none !important;
                -webkit-backdrop-filter: none !important;
                background: rgba(15, 23, 42, 0.95) !important;
            }

            input:focus {
                animation: none !important;
                box-shadow: 0 0 8px rgba(59, 130, 246, 0.4) !important;
            }
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center relative overflow-hidden custom-gradient-bg">
    
    <!-- Hex Pattern Background -->
    <div class="absolute inset-0 hex-pattern"></div>
    
    <!-- Rotating Ring Background -->
    <div class="absolute inset-0 flex items-center justify-center opacity-10">
        <div class="w-[600px] h-[600px] border-4 border-blue-500 rounded-full rotating-ring"></div>
    </div>
    
    <!-- Floating Particles -->
    <div class="absolute top-1/4 left-1/4 w-2 h-2 bg-blue-400 rounded-full particle" style="animation-delay: 0s;"></div>
    <div class="absolute top-1/3 right-1/4 w-3 h-3 bg-blue-300 rounded-full particle" style="animation-delay: 1.5s;"></div>
    <div class="absolute bottom-1/3 left-1/3 w-2 h-2 bg-sky-400 rounded-full particle" style="animation-delay: 3s;"></div>
    <div class="absolute top-1/2 right-1/3 w-3 h-3 bg-blue-400 rounded-full particle" style="animation-delay: 2s;"></div>
    <div class="absolute bottom-1/4 right-1/2 w-2 h-2 bg-blue-300 rounded-full particle" style="animation-delay: 4s;"></div>

    <!-- Main Container -->
    <div class="w-full max-w-5xl px-4 sm:px-6 py-6 sm:py-10 slide-down">
        
        <!-- Top Energy Bar -->
        <div class="energy-bar h-1 sm:h-1.5 bg-gradient-to-r from-transparent via-blue-500 to-transparent rounded-full mb-6 sm:mb-8 pulse-glow"></div>
        
        <!-- Sign In Card -->
        <div class="relative">
            
            <!-- Main Card -->
            <div class="glass-morph border-2 border-pulse relative overflow-hidden rounded-2xl p-6 sm:p-10 md:p-12 shadow-2xl">
                
                <!-- Diagonal Gradient Overlay -->
                <div class="absolute inset-0 bg-gradient-to-br from-blue-900/10 via-transparent to-blue-600/10 pointer-events-none"></div>
                
                <!-- Content -->
                <div class="relative z-10">
                    <!-- Header -->
                    <div class="text-center mb-8 sm:mb-10">
                        <div class="inline-flex items-center justify-center mb-4">
                            <div class="h-px w-12 sm:w-20 bg-gradient-to-r from-transparent to-blue-400"></div>
                            <h1 class="text-xl sm:text-3xl md:text-4xl font-black tracking-widest text-shimmer mx-4 sm:mx-6" style="color: #93c5fd;">
                                PLAYER ACCESS
                            </h1>
                            <div class="h-px w-12 sm:w-20 bg-gradient-to-l from-transparent to-blue-400"></div>
                        </div>
                        <p class="text-xs sm:text-sm md:text-base text-blue-200/80 font-light tracking-wide">
                            AUTHENTICATE TO CONTINUE YOUR MISSION
                        </p>
                    </div>
                    
                    <!-- Form -->
                    <form class="space-y-6 sm:space-y-8 max-w-2xl mx-auto" method="post" action="/loggedin">
                        
                        <!-- Email Input -->
                        <div class="relative">
                            <label class="block text-xs sm:text-sm font-bold mb-2 tracking-wider text-blue-200/90 uppercase">
                                Email Address
                            </label>
                            <input 
                                type="email" 
                                name="email" 
                                placeholder="player@domain.com"
                                required
                                class="w-full px-4 sm:px-6 py-3 sm:py-4 text-sm sm:text-base rounded-xl border-2 transition-all duration-300 bg-slate-900/60 text-blue-100 border-blue-500/40 focus:border-blue-400"
                            />
                            <div class="absolute top-0 right-0 w-2 h-2 bg-blue-400 rounded-full pulse-glow"></div>
                        </div>

                        <!-- Password Input -->
                        <div class="relative">
                            <label class="block text-xs sm:text-sm font-bold mb-2 tracking-wider text-blue-200/90 uppercase">
                                Access Code
                            </label>
                            <input 
                                type="password" 
                                name="password" 
                                placeholder="••••••••••"
                                required
                                class="w-full px-4 sm:px-6 py-3 sm:py-4 text-sm sm:text-base rounded-xl border-2 transition-all duration-300 bg-slate-900/60 text-blue-100 border-blue-500/40 focus:border-blue-400"
                            />
                            <div class="absolute top-0 right-0 w-2 h-2 bg-blue-400 rounded-full pulse-glow" style="animation-delay: 0.5s;"></div>
                        </div>

                        <!-- Submit Button -->
                        <div class="pt-4 sm:pt-6">
                            <input
                                value="Player Return"
                                type="submit"
                                class="w-full px-8 sm:px-12 py-4 sm:py-5 rounded-xl font-black text-sm sm:text-base tracking-widest transition-all duration-300 transform active:scale-95 sm:hover:scale-105 uppercase cursor-pointer" 
                                style="background: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%); color: #ffffff; box-shadow: 0 0 30px rgba(59, 130, 246, 0.6); -webkit-tap-highlight-color: transparent;"
                                onmouseover="this.style.boxShadow='0 0 40px rgba(96, 165, 250, 1)'" 
                                onmouseout="this.style.boxShadow='0 0 30px rgba(59, 130, 246, 0.6)'"
                            >
                        </div>

                        <!-- Back Link -->
                        <div class="text-center pt-4">
                            <a href="/" class="text-xs sm:text-sm text-blue-300/70 hover:text-blue-200 transition-colors duration-300 inline-flex items-center gap-2 uppercase tracking-wider font-semibold">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
                                </svg>
                                New Recruit? Return to Base
                            </a>
                        </div>
                    </form>
                </div>
                
                <!-- Animated Corner Accents -->
                <div class="absolute top-4 left-4 corner-accent border-t-4 border-l-4 border-blue-400/60 rounded-tl-lg"></div>
                <div class="absolute top-4 right-4 corner-accent border-t-4 border-r-4 border-blue-400/60 rounded-tr-lg"></div>
                <div class="absolute bottom-4 left-4 corner-accent border-b-4 border-l-4 border-blue-400/60 rounded-bl-lg"></div>
                <div class="absolute bottom-4 right-4 corner-accent border-b-4 border-r-4 border-blue-400/60 rounded-br-lg"></div>
            </div>
        </div>
        
        <!-- Bottom Energy Bar -->
        <div class="energy-bar h-1 sm:h-1.5 bg-gradient-to-r from-transparent via-blue-500 to-transparent rounded-full mt-6 sm:mt-8 pulse-glow"></div>
        
    </div>

</body>
</html>